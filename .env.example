# Server-side Ask the Sky credentials (set these in Vercel Environment Variables)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini
# Optional PostgreSQL connection string. Without it:
# - local: SQLite in .data/
# - Vercel: ephemeral in-memory preferences
DATABASE_URL=
PORT=5173
COOKIE_SECURE=false
